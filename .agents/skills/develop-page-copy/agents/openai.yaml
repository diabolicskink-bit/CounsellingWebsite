interface:
  display_name: "Page Copy Development"
  short_description: "Plan or write coherent website pages"
  default_prompt: "Use $develop-page-copy to plan or write the requested page scope with natural, persuasive copy and only the documentation the task needs."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Page Copy Development"
  short_description: "Plan pages and create durable copy handoffs"
  default_prompt: "Use $develop-page-copy to plan or write a complete page with $copywriter, keeping the artifact proportional and useful."

policy:
  allow_implicit_invocation: true

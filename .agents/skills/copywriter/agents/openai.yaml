interface:
  display_name: "Evidence-led Editorial Copywriting"
  short_description: "Plan, draft, and challenge credible copy"
  default_prompt: "Use $copywriter to classify the source and artifact, forecast strong, weak, and model-default writing, then draft and adversarially review the copy."

policy:
  allow_implicit_invocation: true

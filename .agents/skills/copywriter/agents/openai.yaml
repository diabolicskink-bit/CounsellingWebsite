interface:
  display_name: "Copy Development"
  short_description: "Plan, shape, draft, and review public copy"
  default_prompt: "Use $copywriter to develop this counselling website copy from a clear brief and considered direction."

policy:
  allow_implicit_invocation: true

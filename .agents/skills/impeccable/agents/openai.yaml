interface:
  display_name: "Impeccable"
  short_description: "Explicit-only frontend design workflow"
  default_prompt: "Use $impeccable explicitly for this frontend task."

policy:
  allow_implicit_invocation: false
